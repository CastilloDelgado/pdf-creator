const bigSize = 10;
const simpleSize = 7;
const headerCellColor = "#cccccc";

module.exports = {
  // font styles

  bidLabel: {
    fontSize: bigSize,
  },

  bigBoldLabel: {
    bold: true,
    fontSize: bigSize,
  },

  simpleLabel: {
    fontSize: simpleSize,
  },
  simpleBoldLabel: {
    fontSize: simpleSize,
    bold: true,
  },

  // table styles
  simpleTable: {
    fontSize: simpleSize,
  },

  headerCell: {
    bold: true,
    fillColor: headerCellColor,
  },
};
